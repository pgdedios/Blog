<h1><%= @article.title %></h1>

<div><%= @article.body %></div>

<ul>
  <li><%= link_to "Edit", edit_article_path(@article) %></li>
  <li><%= link_to "Delete", article_path(@article), 
    data: {
      turbo_method: :delete,
      turbo_confirm: "Are you sure?"
    } %>
  </li>
</ul>

<br>

<%= link_to "Show all comments", article_comments_path(@article) %>

<br>

<h2>Add a comment:</h2>
<%= render "comments/form" %>

<br>
<%= link_to "<< Return to Home", articles_path %>